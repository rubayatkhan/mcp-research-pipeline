# TranscriptAPI.com API key — enables YouTube search, channel, and playlist tools.
# Sign up at https://transcriptapi.com to get 100 free credits.
# Optional: transcript extraction works without this.
TRANSCRIPT_API_KEY=

# Path to NotebookLM auth storage (default shown below).
# Run `notebooklm login` to authenticate before starting the server.
# NOTEBOOKLM_STORAGE_PATH=~/.notebooklm/storage_state.json
